import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'twitter_ionic_mobileapp',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
